import type { ReactNode } from "react";

export const metadata = {
  title: "Create a producer account",
  description: "Create your organisation and start from published GLOBALG.A.P. knowledge.",
};

export default function SignupLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
